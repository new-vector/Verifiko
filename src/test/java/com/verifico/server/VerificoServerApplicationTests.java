package com.verifico.server;

import org.junit.jupiter.api.Test;
// import org.springframework.boot.test.context.SpringBootTest;

// @SpringBootTest removing because it causes the whole app to load when doing unit tests
// implement test containers for CI so we don't need to spin up db manually.
class VerificoServerApplicationTests {

	@Test
	void contextLoads() {
	}

}
